import { el } from '../element.js';

export function renderComponent(component, onClickHandler) {

  const componentElement = Null;
  for (const item of component) {
    const type = item.type;
  }


}
